/**
 * Video Interface
 * It is a estruct to video clips
 * @interface Video
 */
export interface Video {
    /**
     * id video clip
     */
    id: string,
    /**
     * title video clip
     */
    title: string,
    /**
     * timeStart video clip
     */
    timeStart: string,
    /**
     * timeEnd video clip
     */
    timeEnd: string
}